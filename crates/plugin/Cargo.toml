[package]
name = "mangav-plugin"
version = "0.0.1"
edition = "2021"
description = "MangaV 基于 WASM / Extism 的插件系统"

[dependencies]
mangav-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
log = "0.4"

[package]
name = "novella-media"
version = "0.0.1"
edition = "2021"
description = "Novella FFmpeg 硬件加速视频渲染引擎与音轨合成器"

[dependencies]
novella-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"

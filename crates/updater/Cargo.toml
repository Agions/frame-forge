[package]
name = "mangav-updater"
version = "0.0.1"
edition = "2021"
description = "MangaV 自动更新与签名验证服务"

[dependencies]
mangav-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
log = "0.4"
